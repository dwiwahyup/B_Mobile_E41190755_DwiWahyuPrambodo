<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.belajar_layout">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Belajar_layout">
        <activity android:name=".materialdesignlayout">

        </activity>
        <activity android:name=".landing_page">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".scrollviewhorizontal" />
        <activity android:name=".scrollview" />
        <activity android:name=".table_layout" />
        <activity android:name=".frame_layout" />
        <activity android:name=".constraint_layout" />
        <activity android:name=".relative_layout" />
        <activity android:name=".linear_layout" />

    </application>

</manifest>